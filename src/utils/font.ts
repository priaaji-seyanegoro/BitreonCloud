import { Aldrich } from "next/font/google";


export const kanit = Aldrich({
  weight: ["400"],
  subsets: ["latin"],
  display: "swap",
  preload: true,
})

export const kanitBold = Aldrich({
  weight: ["400"],
  subsets: ["latin"],
  display: "swap",
  preload: true,
})
