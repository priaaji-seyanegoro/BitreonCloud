import { Kanit } from "next/font/google";


export const kanit = Kanit({
  weight: ["400"],
  subsets: ["latin"],
  display: "swap",
  preload: true,
})

export const kanitBold = Kanit({
  weight: ["800"],
  subsets: ["latin"],
  display: "swap",
  preload: true,
})
